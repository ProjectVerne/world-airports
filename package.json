{
  "name": "world-airports",
  "version": "0.0.0",
  "private": true,
  "description": "Open dataset of airports worldwide (v2 identity model).",
  "license": "CC0-1.0",
  "scripts": {
    "validate": "node scripts/validate.js",
    "validate:changed": "node scripts/validate.js --changed",
    "import:ourairports": "node scripts/import/from-ourairports.js",
    "export:codes": "node scripts/export/to-codes.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
